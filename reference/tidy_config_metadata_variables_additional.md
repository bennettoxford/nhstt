# Tidy configuration for annual metadata variables (additional)

Tidy configuration for annual metadata variables (additional)

## Usage

``` r
tidy_config_metadata_variables_additional()
```

## Value

List containing tidy configuration for metadata_variables_additional
dataset

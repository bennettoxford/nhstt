# Retrieve tidy configuration for a dataset/frequency combination

Retrieve tidy configuration for a dataset/frequency combination

## Usage

``` r
get_tidy_config(dataset, frequency)
```

## Arguments

- dataset:

  Character, dataset name (e.g., "key_measures")

- frequency:

  Character, "annual" or "monthly"

## Value

Named list of configuration values

# Get dataset version

Get dataset version

## Usage

``` r
get_dataset_version(dataset, frequency)
```

## Arguments

- dataset:

  Character, specifying dataset name (e.g., "key_measures",
  "activity_performance")

- frequency:

  Character, specifying report frequency ("annual" or "monthly")

## Value

Character version string

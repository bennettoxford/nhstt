# Check if cache file exists

Check if cache file exists

## Usage

``` r
resolve_cache_file(file_path, use_cache)
```

## Arguments

- file_path:

  Character, path to cache file

- use_cache:

  Logical, whether to use cached data if present

## Value

Character path if cache exists and should be used, NULL otherwise

# Filter rows

Filters rows based on configuration

## Usage

``` r
filter_rows(df, filter_config)
```

## Arguments

- df:

  Tibble, specifying data to filter

- filter_config:

  Named list, specifying filter values (e.g., list(org_type =
  c("Provider")))

## Value

Filtered tibble

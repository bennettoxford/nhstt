# Validate raw configuration structure

Validate raw configuration structure

## Usage

``` r
validate_raw_config(config)
```

## Arguments

- config:

  List, specifying parsed raw configuration

## Value

Invisible TRUE if valid, aborts otherwise

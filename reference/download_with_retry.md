# Download data from URL with retry logic

Download data from URL with retry logic

## Usage

``` r
download_with_retry(url, file)
```

## Arguments

- url:

  Character, specifying URL to download from

- file:

  Character, specifying destination file path

# Get raw downloads metadata file path

Returns the path to the JSON file that stores download metadata for raw
data

## Usage

``` r
get_raw_downloads_json_path(frequency)
```

## Arguments

- frequency:

  Character, specifying report frequency ("annual" or "monthly")

## Value

Character path to .downloads.json file

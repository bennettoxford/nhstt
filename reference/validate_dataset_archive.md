# Validate dataset archive reference

Validate dataset archive reference

## Usage

``` r
validate_dataset_archive(dataset_name, dataset, config)
```

## Arguments

- dataset_name:

  Character, dataset name

- dataset:

  List, dataset configuration

- config:

  List, full configuration for archive lookups

## Value

Invisible TRUE if valid, aborts otherwise

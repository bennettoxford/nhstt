# Tidy configuration for annual metadata variables (main)

Tidy configuration for annual metadata variables (main)

## Usage

``` r
tidy_config_metadata_variables_main()
```

## Value

List containing tidy configuration for metadata_variables_main dataset

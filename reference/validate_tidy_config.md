# Validate tidy configuration structure

Validate tidy configuration structure

## Usage

``` r
validate_tidy_config(config)
```

## Arguments

- config:

  List, specifying parsed tidy configuration

## Value

Invisible TRUE if valid, aborts otherwise

# Package attach hook

Silently sets up package on first load, only shows message if
initialization fails

## Usage

``` r
.onAttach(libname, pkgname)
```

## Arguments

- libname:

  Library name

- pkgname:

  Package name

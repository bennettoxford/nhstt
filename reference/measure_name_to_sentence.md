# Convert raw measure names to sentence case for figure labels

Convert raw measure names to sentence case for figure labels

## Usage

``` r
measure_name_to_sentence(x)
```

## Arguments

- x:

  Character vector of measure names (e.g., "ended_completed")

## Value

Character vector in sentence case (e.g., "Completed")

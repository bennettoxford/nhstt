# Validate individual dataset configuration

Validate individual dataset configuration

## Usage

``` r
validate_dataset_config(dataset_name, dataset, config)
```

## Arguments

- dataset_name:

  Character, specifying dataset name

- dataset:

  List, specifying dataset configuration

- config:

  List, specifying full configuration (for archive lookups)

## Value

Invisible TRUE if valid, aborts otherwise

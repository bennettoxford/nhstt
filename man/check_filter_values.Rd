% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dev-utils.R
\name{check_filter_values}
\alias{check_filter_values}
\title{Check that configured filter values match rows in the tidied data}
\usage{
check_filter_values(df, dataset, frequency)
}
\arguments{
\item{df}{Tibble, tidied data combined across all periods}

\item{dataset}{Character, raw dataset name}

\item{frequency}{Character, "annual" or "monthly"}
}
\value{
Invisibly returns \code{df}
}
\description{
Developer tool, run as part of \code{\link[=build_tidy_data]{build_tidy_data()}}. Filtering happens per
period, and a filter value can legitimately be absent from individual
periods (e.g. SubICB only appears in monthly data from mid-2022). But a
value that matches no rows across all periods combined is almost certainly
misspelled in the tidy config, so this errors rather than silently
publishing a parquet with those rows missing.
}
\keyword{internal}

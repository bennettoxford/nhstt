% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-tidy-dataset.R
\name{get_tidy_dataset}
\alias{get_tidy_dataset}
\title{Get a pre-built tidy dataset}
\usage{
get_tidy_dataset(dataset, periods = NULL, use_cache = TRUE)
}
\arguments{
\item{dataset}{Character, dataset name as listed in tidy_data_sources.yml}

\item{periods}{Character vector of periods, or NULL for all periods}

\item{use_cache}{Logical, whether to use cached data if available}
}
\value{
Tibble, ordered most-recent period first
}
\description{
Shared core for all user-facing \verb{get_*()} functions. Looks up the dataset
in \code{tidy_data_sources.yml}, downloads the pre-built parquet from the
GitHub Release if the cache is missing or stale, and filters to the
requested periods.
}
\details{
Requested periods are validated against the periods actually present in
the downloaded data, so a period that exists in the developer config but
not yet in the published parquet raises an error instead of silently
returning zero rows.
}
\keyword{internal}

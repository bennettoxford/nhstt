% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhstt_setup.R
\name{nhstt_setup}
\alias{nhstt_setup}
\title{Setup R package and download NHS TT reports}
\usage{
nhstt_setup(
  datasets = c("key_measures", "medication_status", "therapy_type", "effect_size"),
  force = FALSE
)
}
\arguments{
\item{datasets}{Character vector, specifying which datasets to process.
Available datasets can be listed with \code{\link[=nhstt_info]{nhstt_info()}}.
Default includes all four main datasets.}

\item{force}{Logical, specifying whether to re-download even if data exists.
Default is FALSE.}
}
\value{
Invisible character, the path to cache directory
}
\description{
Downloads and tidies NHS Talking Therapies data for all available periods.
Run once to initialise the package data. After setup, datasets can be accessed
directly by name (e.g., \code{key_measures}).
}
\examples{
\donttest{
# Download all default datasets (recommended)
nhstt_setup()

# Download specific datasets only
nhstt_setup(datasets = c("key_measures", "effect_size"))

# Force re-download to get latest data
nhstt_setup(force = TRUE)

# After setup, access datasets directly
key_measures
medication_status
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw-annual-legacy-format.R
\name{read_legacy_format_outcome_csv}
\alias{read_legacy_format_outcome_csv}
\title{Read a clinical outcome breakdown from a CSV pack (2015-16 / 2016-17)}
\usage{
read_legacy_format_outcome_csv(zip_path, pattern)
}
\arguments{
\item{zip_path}{Character, specifying path to the ZIP archive}

\item{pattern}{Character, specifying regex identifying the CSV file inside the archive}
}
\value{
Tibble with the dimension column followed by all available clinical
outcome measure columns
}
\description{
Handles tables: \verb{table-9b} (ethnicity outcomes), \verb{table-10b} (sexual
orientation), \verb{table-11b} (disability), \verb{table-12b} (religion),
\verb{table-13b} (IMD), \code{table-14} (LTC).
}
\details{
The dimension category is always in column 3.  Column names differ between
tables and years; all variants are merged via \code{coalesce()} after
\code{clean_str()}.

Known data quirks handled:
\itemize{
\item 2016-17 \code{table-14} typo: \code{NoRelibableChange}
\item 2016-17 \verb{table-12b} typo: \code{StartedNoCaseness}
\item 2016-17 \verb{table-9b}/\verb{table-10b}: uses \verb{All*} prefix column naming
(e.g. \code{AllImprovement}) instead of the 2015-16 \verb{ALL_*} style
}
}
\keyword{internal}

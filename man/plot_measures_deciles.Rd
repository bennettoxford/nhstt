% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_measures_deciles}
\alias{plot_measures_deciles}
\title{Plot decile charts for each measure, optionally highlighting services}
\usage{
plot_measures_deciles(
  measures,
  highlight_services = NULL,
  hide_service_names = FALSE,
  ncol = 2,
  panel_tags = FALSE,
  measure_order = NULL
)
}
\arguments{
\item{measures}{Tibble, as returned by \code{\link[=create_measures]{create_measures()}}, filtered to the
measures to plot}

\item{highlight_services}{Character vector of org codes to highlight (e.g.,
\code{c("RNUDT", "RTQ")}). Default NULL (no services highlighted)}

\item{hide_service_names}{Logical, label highlighted services "Service A",
"Service B", ... instead of their service name. Default FALSE}

\item{ncol}{Integer, number of facet columns. Default 2}

\item{panel_tags}{Logical, prefix panels with "A:", "B:", ... Default FALSE}

\item{measure_order}{Character vector of measure IDs giving the panel
order. Default NULL (panels ordered by measure ID)}
}
\value{
A ggplot object, so labels and theme can be adjusted with \code{+}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Shows the distribution of ratios across all services as decile bands (10th
to 90th percentile, darker closer to the median), facetted by measure.
Selected services can be highlighted as coloured lines, labelled with their
service name by default or with generic "Service A", "Service B", ... labels
when service names should not be shown (e.g. in a blog post).
}
\examples{
\dontrun{
# highlighted services labelled with their names
df_clean |> plot_measures_deciles(highlight_services = c("RNUDT", "RTQ"))

# generic labels when service names should not be shown
df_clean |> plot_measures_deciles(highlight_services = c("RNUDT", "RTQ"), hide_service_names = TRUE)
}
}
